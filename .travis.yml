language: c
compiler:
  - gcc
  - clang
before_script:
  - git clone git://github.com/LuaDist/lua.git lua
  - cd lua
  - git checkout 5.2.2
  - cmake . && make
  - cd ../
script: make test CFLAGS="-I./lua/ -I./lua/src"
