socket.select?
sock:accept
sock:bind
sock:listen

buffer
======
    sock:read, don't malloc str buf even if recv error

Buffered IO
===========
    sock:readbyte
    sock:readstring
    sock:readline
    sock:writebyte
    sock:writestring
